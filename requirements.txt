# Belle main venv. Install:  pip install -r requirements.txt
# Camera/YOLO stack: requirements-camera.txt

# Voice: STT/LLM (Groq) + TTS (ElevenLabs)
groq==0.37.1
elevenlabs
sounddevice==0.5.5
numpy==2.4.2

# Wake word (Edge Impulse)
edge_impulse_linux==1.2.2

# Vision (Gemini + OpenCV)
google-genai
opencv-python==4.13.0.92

# RAG (health tool)
faiss-cpu
fastembed
pypdf

# Backend / internal API
fastapi
uvicorn
pydantic==2.12.5
httpx==0.28.1
requests==2.32.5

# Integrations: Firestore, Telegram, scheduler, news
firebase-admin
python-telegram-bot[job-queue]
apscheduler
feedparser

# Config (.env)
python-dotenv==1.2.2
